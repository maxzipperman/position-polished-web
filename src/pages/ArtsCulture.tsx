import { IndustryPage } from '@/components/IndustryPage';
import { artsCultureData } from '@/data/industries';

const ArtsCulture = () => {
  return <IndustryPage data={artsCultureData} />;
};

export default ArtsCulture;
