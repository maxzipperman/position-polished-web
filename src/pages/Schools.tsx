import { IndustryPage } from '@/components/IndustryPage';
import { schoolsData } from '@/data/industries';

const Schools = () => {
  return <IndustryPage data={schoolsData} />;
};

export default Schools;
