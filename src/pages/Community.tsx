import { IndustryPage } from '@/components/IndustryPage';
import { communityData } from '@/data/industries';

const Community = () => {
  return <IndustryPage data={communityData} />;
};

export default Community;
