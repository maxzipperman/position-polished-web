import { IndustryPage } from '@/components/IndustryPage';
import { parksRecData } from '@/data/industries';

const ParksRec = () => {
  return <IndustryPage data={parksRecData} />;
};

export default ParksRec;
